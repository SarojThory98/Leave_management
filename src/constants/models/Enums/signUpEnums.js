const USER_TYPE_ENUM = {
	MANAGEMENT: 0,
	HR: 1,
	EMPLOYEE: 2,
};

// url enums
const USER_URL_ENUM = {
	MANAGEMENT_TYPE: "management",
	HR_TYPE: "hr",
	EMPLOYEE_TYPE: "employee",
};

module.exports = {
	USER_TYPE_ENUM,
	USER_URL_ENUM,
};
